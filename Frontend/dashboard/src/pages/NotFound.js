import { Typography } from "@mui/material";

const NotFound = () => {
  return (
    <Typography variant="h3" sx={{ textAlign: "center" }}>
      Page not found
    </Typography>
  );
};

export default NotFound;
